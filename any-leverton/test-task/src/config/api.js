const root = 'https://jsonplaceholder.typicode.com';

export default API = {
  posts: `${root}/posts/`,
  users: `${root}/users/`,
  albums: `${root}/albums`,
  photos: `${root}/photos`,
  comments: `${root}/comments`,
}